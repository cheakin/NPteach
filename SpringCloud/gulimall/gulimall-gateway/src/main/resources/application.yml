spring:
  cloud:
    gateway:
      routes:
        - id: baidu_route
          uri: https://www.baidu.com
          predicates:
            - Query=url, baidu  # Query指请求参数
        - id: qq_route
          uri: https://www.qq.com
          predicates:
            - Query=url, qq